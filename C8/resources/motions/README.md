# Local AMP Motion Data

Put C7-generated AMP-compatible `.npz` expert clips here in `walk/`, `run/`,
`mixed/`, and optionally `dance/`. The binary motion files are intentionally
ignored; see [`docs/RESOURCES.md`](../../docs/RESOURCES.md) for required names.
