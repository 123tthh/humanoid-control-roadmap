# C6：教师—学生蒸馏全身运动跟踪框架

本章使用课程提供的 mjlab/uv 工程，与 C4 一样是独立 Python 环境；C1 的 Isaac Sim、Isaac Lab 和训练进程均不在其修改范围内。

入口命令：

```bash
cd /home/gtk/UNITREE/C6
./scripts/c6.sh preflight
./scripts/c6.sh setup
```

完成 `setup` 只会创建 C6 私有环境。待课程工程到位后，训练和 Viser 回放须使用其源码中定义的命令，框架不根据 PDF 猜测 API。
